# Servidor Apache com Docker

Este projeto configura um servidor Apache usando Docker Compose.

## 🚀 Como executar

1. Clone este repositório
2. Execute o comando:
   ```bash
   docker-compose up -d